# notebooks/

Exploratory notebooks only. The deliverable is the Python package in `src/`
driven by `main.py`; nothing in this folder is required to reproduce the
results.

The professor's data-loading starter notebooks, which used to live here, are
kept verbatim in `professor_solution/`.
